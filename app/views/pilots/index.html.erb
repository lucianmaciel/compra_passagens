<h1>Lista de Pilotos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Endereco</th>
    <th>Telefone</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pilots.each do |pilot| %>
  <tr>
    <td><%= pilot.nome %></td>
    <td><%= pilot.endereco %></td>
    <td><%= pilot.telefone %></td>
    <td><%= link_to '[Mostrar Detalhes]', pilot %></td>
    <td><%= link_to '[Editar]', edit_pilot_path(pilot) %></td>
    <td><%= link_to '[Apagar]', pilot, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '[Novo Piloto]', new_pilot_path %>
<a href="index">[Página Inicial]</a>
